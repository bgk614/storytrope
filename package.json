{
  "name": "storytrope",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "format": "prettier --write \"*.{md,mjs,json,yaml,yml}\" && pnpm -r run format"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.0",
    "prettier": "^3.4.2"
  }
}
