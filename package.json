{
  "name": "storytrope",
  "private": true,
  "scripts": {
    "prepare": "husky || true",
    "format": "prettier --write .",
    "dev": "pnpm -r --parallel run dev"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.0",
    "prettier": "^3.4.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "bcrypt",
      "prisma",
      "@prisma/client",
      "@prisma/engines"
    ]
  }
}
